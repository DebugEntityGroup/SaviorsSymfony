{% if is_granted('ROLE_ADMIN') %}
{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="h">Ajouter un nouveau Type de réclamation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Ajouter" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('raisons_index') }}">Liste des raisons</a>
        </li>
    </ul>
    <style>
        .h{
            padding: 20px;
        }
    </style>
{% endblock %}
{% endif %}
