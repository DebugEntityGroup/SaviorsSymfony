{% extends 'layout.html.twig' %}
{% block title %} Modifier une réclamation {% endblock %}
{% block bground %}
{% endblock %}
{% block content %}
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
    <h1>Modifier Réclamation</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Modifier" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('reclamation_index', { 'idR': reclamation.id }) }}"><button type="button" class="btn btn-sm btn-outline-info" >Liste des réclamation</button></a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
    <br>

{% endblock %}
